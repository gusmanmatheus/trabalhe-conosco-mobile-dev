package com.example.picpaytest

import com.example.picpaytest.data.remote.ServiceRequest
import com.example.picpaytest.feature.contacts.ContactsContract
import com.example.picpaytest.feature.contacts.ContactsPresenter
import org.junit.Before
import org.junit.Test
import org.mockito.Mock
import org.mockito.Mockito
import org.mockito.Mockito.inOrder
import org.mockito.MockitoAnnotations

class ContactsPresenterTest {
    /*@Mock val view: ContactsContract.View
      @var serviceRequest: ServiceRequest

    @Before
    fun setUp(){
       MockitoAnnotations.initMocks(this)
        private val presenter = ContactsPresenter(view,serviceRequest)

    }

    @Test
    fun `test get users with succes`(){

        presenter.getUsers()

    }*/
}